---
// Статус-блок: БЕЗ синей плашки, кнопок и ссылок (контакты живут в hero).
// Просто спокойный текст на белом, по центру: сайт строится + оффер.
---

<section class="contact" id="contact">
  <div class="container">
    <p class="contact__lead">
      Сейчас создаю и украшаю этот сайт. Готов сделать первую концепцию
      вашего сайта бесплатно — пишите.
    </p>
  </div>
</section>

<style>
  .contact { padding: var(--section-pad-y) 0; background: var(--color-paper-white); }
  .contact__lead {
    font-size: var(--text-lead);
    line-height: var(--lh-lead);
    font-weight: var(--fw-medium);
    color: var(--color-ink-black);
    max-width: var(--measure);
    margin: 0 auto;
    text-align: center;
  }
</style>
