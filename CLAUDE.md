# Дизайн-система портфолио

Живой документ. Все директивы по дизайну фиксируем здесь. Полигон — `src/pages/index.astro` (видимый скелет всех секций).

## Правила работы

- **Скорость > церемония.** Меньше TodoWrite, меньше пояснений, больше кода. Один Write на файл, файлы параллельно.
- **HMR работает сам.** Не пингую сервер, не перезапускаю без причины.
- **Фиксируем здесь** каждую утверждённую правку (палитра, типографика, токен, компонент, исключения).

## ЖЁСТКИЕ ПРАВИЛА (не нарушать)

1. **Текст:** на белом фоне — только чисто чёрный `#000000`; на синих секциях (Hero, Contact) — чисто белый `#ffffff`. Никакого серого, ни в каком виде.
2. **Никаких прозрачностей** (`rgba` с alpha, `opacity` на цветах, `color-mix`). Только сплошные тона. Исключение — тень пилюли-навбара.
3. **Заголовки — Inter 900 (black).** Всегда.
4. **Акцент — синий `#3B73B9`** (бывший оранжевый выкинут полностью). Используется как highlight-плашка и акцентные цифры/элементы.
5. **Без нумерованных бирок** над секциями («02 / Работы» и т.п.) — убраны.
   6a. **Без КАПСА.** `text-transform: uppercase` запрещён везде. Текст как написан.
6. Каждое новое дизайн-решение **сразу фиксируется здесь.**
7. **Без висячих предлогов/союзов.** Короткое слово (предлог, союз, 1–2 буквы) не остаётся в конце строки. Делается АВТОМАТИЧЕСКИ и глобально: `src/lib/typo.ts` (логика — клеит неразрывным пробелом ` `) + скрипт-типограф в `Layout.astro` проходит по всем текстовым узлам страницы (на load и `astro:page-load`). Руками в шаблонах оборачивать НЕ нужно — работает само на всём сайте и будущих текстах. Переводы строк (`\n`, разбивка абзацев) не трогает.
8. **Все видео — максимально оптимизированы. ВСЕГДА.** Видео у нас только как обложка (на главной в галерее + та же обложка тянется на hero страницы кейса). Никакого звука, никаких наворотов — просто зацикленный лёгкий ролик, который грузится мгновенно. Требования к КАЖДОМУ видео перед добавлением в `public/`:
   - **Без звука** — аудиодорожка вырезается (`-an`).
   - **H.264** (`libx264`, `yuv420p`, `+faststart`), зациклено в разметке (`muted loop playsinline preload="none"`, автоплей только в зоне видимости).
   - **Сжимать жёстко:** `-crf 30 -preset veryslow -r 24`, ширина ≤ 1280px для hero-обложек (`scale='min(1280,iw)':-2`). Цель — обложка весит ~сотни КБ, не мегабайты.
   - Рецепт (эталон): `ffmpeg -i in.mp4 -an -c:v libx264 -profile:v high -pix_fmt yuv420p -crf 30 -preset veryslow -r 24 -vf "scale='min(1280,iw)':-2" -movflags +faststart out.mp4`
   - Если после сжатия файл не стал заметно легче — гнать сильнее (crf 32+), но всегда проверять, что визуально обложка ок.

## Шрифты

- **ТОЛЬКО Inter, два веса: Medium 500 и Black 900.** Больше ничего не грузим.
  - Body / метки / ссылки / **кнопки** → 500. Заголовки → 900. Токены весов — алиасы только на 500 или 900.
- **Моноширинный исключён** (`--font-mono` = алиас на `--font-sans`).
- **Разрядка (letter-spacing) = 0 везде.** Все tracking-токены `--tr-*` обнулены, literal-значения тоже.
- Display-шрифт не используем — Inter 900 тянет все заголовки.

## Палитра

_Источник истины — `src/styles/global.css` (`@theme`)._

| Токен                 | HEX         | Роль                                                     |
| -------------------------- | ----------- | ------------------------------------------------------------ |
| `--color-ink-black`      | `#000000` | Текст (только чёрный), рамки           |
| `--color-paper-white`    | `#ffffff` | Канва, текст на синем/чёрном          |
| `--color-accent`         | `#3B73B9` | Акцент: highlight, цифры, синие секции |
| `--color-accent-700`     | `#2C568B` | Hover / нажатие                                       |
| `--color-accent-900`     | `#1E3A5D` | Самый тёмный синий                           |
| `--color-faint-slate`    | `#f8fafc` | Тихий фон                                            |
| `--color-lavender-mist`  | `#e2e9f3` | Поверхность карточек                      |
| `--color-blue-gray-mist` | `#c5d3e8` | Плашки                                                 |
| `--color-cream-wash`     | `#fffde6` | Тёплый акцент-фон                             |
| `--color-sky-tint`       | `#c4edff` | Светлые плашки                                  |

`--color-signal-orange` оставлен как **алиас на `--color-accent`** (синий), чтобы старые ссылки не ломались.

## Типографическая шкала (токены)

| Токен         | Размер | Роль                                   |
| ------------------ | ------------ | ------------------------------------------ |
| `--text-display` | 120px        | Гигантский экран            |
| `--text-h1`      | 72px         | Hero, Contact                              |
| `--text-h2`      | 48px         | Заголовки секций            |
| `--text-h3`      | 24px         | Подзаголовки, карточки |
| `--text-lead`    | 20px         | Лид-абзац                          |
| `--text-body`    | 16px         | Основной текст                |
| `--text-caption` | 12px         | Mono-метки                            |

Утилиты: `.t-display`, `.t-h1`…`.t-body`, `.mono`. Тянут токены, хардкодов нет.

## Спейсинг

Шкала `--spacing-4 … --spacing-120`. Ритм секций: `--section-pad-y` (64px по умолчанию; секции часто 96px).

## Компоненты

_Каждый компонент фиксируем как: роль · разметка · стили · поведение._

### Nav — плавающая пилюля (Dynamic Island)

Чёрная пилюля `border-radius: 999px`, висит над контентом (`position: sticky; top: 16px`, тень). Лого + цветной значок слева, ссылки по центру (десктоп), белая кнопка «Связаться» + бургер справа. На мобиле ссылки прячутся → бургер открывает выпадающее меню-таблетку.

### Hero

**Светлый фон** (`--color-paper-white`). Текст — чисто чёрный, **выровнен по центру**. Две части:
1. **Имя «Я — Никита Чендев» — бирка-эйброу** над заголовком (`clamp(18px, 4.6vw, 34px)`, Inter 900, чёрный). Потолок 34px — чтобы на ПК пропорция к заголовку не была мелкой (на мобилке `4.6vw` держит пропорцию сам).
2. **Заголовок «Создаю потрясающие сайты для малого и среднего бизнеса»** — крупный, **тянется к полям** (fluid `clamp(34px, 8.8vw, 76px)`), Inter 900, `line-height 1.03`. **`text-wrap: balance`** — равномерно раскидывает строки, нет строк-сирот. Форсированных `<br>` НЕТ (они ломали мобилку — одинокое «сайты»), переносы естественные.

**Лид** — чисто чёрный (`#000`), `line-height 1.2`, вес 500, весь текст обычным начертанием. Пилюль/выделений в лиде нет (пробовали «один год» в синей пилюле — отклонено). Кнопок нет. _(Фото `public/hero-lying.png` — лежащий парень, прозрачный PNG — пока из Hero убрано, файл лежит в public на потом.)_

### Works (галерея + фильтр) — ГЛАВНЫЙ БЛОК

Вдохновение — artlebedev.ru. **Сетка СНЯТА ЗАМЕРОМ с живого сайта** (chrome-devtools MCP, `getBoundingClientRect` на уже разложенном Vue-гриде `#als-portfolio-v3`). Числа ниже — реальные, не на глаз.

**Модель сетки artlebedev v5 (источник истины, замер на контейнере 1490px):**

- Ряд = `.p-v3-grid-line` (`display:block`); плитки = `inline-block; vertical-align:top` (по верху), ширина задана явно.
- **Юнитная сетка.** Два размера: `middle` = 2 юнита (740px ≈ 49.6%), `small` = 1 юнит (370px ≈ 24.8%). middle ровно ×2 от small.
- **Aspect-ratio (главное):** middle = 740/570 = **1.297** (ландшафт ~4:3); small = 370/570 = **0.649** (портрет ~2:3). База 1 юнита = `370/570 = 0.649`.
- В ряду плитки по верху; smalls добиты нижним отступом → внешние боксы выровнены ~570px → ряды стыкуются БЕЗ ДЫР.
- **Динамика по принципу контраста (наше решение):** сумма юнитов в ряду варьируется → разная высота рядов. Меньше сумма юнитов → шире юнит → ВЫШЕ ряд. У нас: ряд из 4 юнитов ≈532px, из **3** ≈713px (высокий, разреженный), из **5** ≈423px (низкий, плотный). Контраст задаём ОСОЗНАННО: размер (крупное × мелкое) + цвет (тёплый `#7C4A1E` × холодные синие). Текущие 4 ряда: `[m,s,s]`(4) · `[s,s,m]`(4) · `[m,s]`(3, высокий) · `[s,s,m,s]`(5, низкий).
- **Про «отступ первой карточки»:** реального отступа нет (всё на 15px). Это бенто-смещение швов между рядами (ряд `[m,s,s]` режет на 50/75%, ряд `[s,s,m]` на 25/50% — швы не совпадают). Это фича, а не баг; ритм держим осознанным через контраст.

**Наша реализация (`works.ts` + `Works.astro`):**

- У работы: `size: 'small'|'middle'` и `row`. Компонент группирует по рядам → `.row` (flex) → `.tile`.
- Плитка: `flex: var(--u) 1 0` (`--u` = юниты: small 1, middle 2) + `aspect-ratio: calc(var(--u) * 0.649)`. Т.к. AR ∝ юнитам, **все плитки ряда автоматически одной высоты** → дыр нет, fixed-высот рядов НЕ нужно. Проверено: рендер даёт middle 49.5%/AR 1.298, small 24.7%/AR 0.649 — копия оригинала.
- **Название ВНУТРИ плитки**, снизу (absolute), белым. Под ним **градиент** `linear-gradient(to top, surface 0% → color-mix 80% на 23% → transparent 83%)`. Снято с оригинала: у них `to bottom 0→0%(17%)→0.8(77%)→solid(100%)` в БРЕНД-ЦВЕТЕ плитки (инлайнится в каждую). Суть: картинка утопает в свой же цвет, плоская чистая зона сверху, solid (alpha 1) только у самого низа → белый текст читается при любой картинке. Исключение из «без прозрачностей» — градиент для читабельности разрешён.
- `surface` — насыщенный/тёмный цвет (бренд-цвет работы), он же красит градиент. Сейчас плейсхолдер вместо картинки/видео.
- **Посадка медиа — поле `fit` ('cover' | 'contain', по умолч. 'cover'):**
  - `cover` (дефолт) — full-bleed, обрезка под край. Под видео и скрины во всю плитку. Картинку готовить под AR плитки (middle 1.3 / small 0.65), экспорт 2× (middle 1480×1140, small 740×1140; под крупные мониторы — 2000×1540 / 1000×1540).
  - `contain` — **объект по центру, `surface`-цвет заливает свободные поля** (лого / мокап / прозрачный PNG). Реализация: `.tile[data-fit="contain"] .tile__fill { object-fit: contain; padding: 10% 10% 16% }` (низ — под зону градиента+подписи, поэтому padding-bottom больше). AR плитки для картинки тут не важен — объект вписывается как есть. Готовить: **прозрачный PNG/WebP**, длинная сторона 1200–1600px, с небольшим внутренним воздухом.
- **Плоско, без эффектов** (проверено: у artlebedev `transition: 0s`, нет hover-зума/тени/подъёма). У нас тоже убран `translateY` по ховеру — «живость» даёт сам медиа, не CSS.
- **База под видео:** `video` + `poster` → `<video muted loop playsinline preload="none">`, source `.mp4`, full-bleed cover. Автоплей **только в зоне видимости** (IntersectionObserver, threshold 0.25, preload включается лениво при входе) — как у Лебедева. `prefers-reduced-motion: reduce` → видео не запускается, остаётся `poster`. Гиф/статика — поле `image`. Приоритет медиа: video → image → только surface.
  **Поля узкие, сайт полноширинный:** контейнер БЕЗ max-width (`--container-page: 100%`), боковые поля везде **15px**. Галерея — те же 15px, gap между плитками 8px (десктоп) / 6px (≤1079px).
  Адаптив: AR тянет высоту от ширины сам (fixed-высот нет). **Мобилка ≤767px — модель artlebedev (проверено по их сайту):** `middle` во всю ширину (ландшафт, AR 1.5), `small` по **2 в ряд** (портрет, AR 0.66) — НЕ простая стопка. Реализация: `.works__grid` → `display:grid; grid-template-columns:1fr 1fr; grid-auto-flow:row dense`, `.row` → `display:contents` (плитки текут в общий грид сплошным потоком, small'ы парятся даже между исходными рядами; `dense` убирает дыры от одиночных), `middle` → `grid-column: span 2`. В гриде flex-свойства игнорируются, высоту даёт aspect-ratio (важно: раньше в flex-колонке `flex-basis:0` по вертикали схлопывал высоту в 0 → баг «белое место»).
  **Заголовка «Работы» нет** и **фильтр-чипов нет** — секция начинается сразу с галереи. (Чипы-фильтр по категориям убраны полностью: разметка, CSS `.works__chip`/`.works__filter`, JS `bindFilter`, экспорт `categories` из `works.ts`. Поле `category` в `works.ts` оставлено на будущее.)

### Страница кейса (`src/pages/works/[slug].astro`) — ПЕРЕИСПОЛЬЗУЕМАЯ
Одна структура на все 12 кейсов; наполнение — под каждый через необязательные поля `Work` (`siteUrl`, `overview`, `blocks`). Кейсы без полей корректно падают на фолбэк (hero + CTA + навигация).
- **Hero на весь экран (`100svh`, full-bleed — единственное исключение из полей 15px).** Видео `0624.mp4` (приоритет video → image → surface), автоплей в зоне видимости (IntersectionObserver 0.25), пауза вне, `prefers-reduced-motion` → poster без плея. Поверх: название белым (h1) + теги·год, стрелка «↓» снизу (bob-анимация, гаснет при reduce-motion). Градиент снизу `rgba(0,0,0,.6→.15→0)` — исключение ради читабельности.
- **Кнопка «← к работам» — `position: fixed` в шапке (top 14px / left 15px), едет со скроллом по всей странице.** Крупная (`--text-h3`), Inter 900. **`mix-blend-mode: difference`** — инвертирует фон под собой: над тёмным видео белая, над белой полосой чёрная, над цветной — инверт. Текст «проходит» сквозь контент и ВСЕГДА контрастен (нет фикс-цвета, читается на любом фоне). Разрешённое исключение из «без прозрачностей/blend»: blend здесь ради читабельности и эффекта, зафиксирован осознанно. (Буквальный `hard-light` отклонён: с белым текстом на белых полосах давал белое-на-белом → пропадал.)
- **Мета-строки НЕТ.** Вместо неё в обзоре — крупная акцентная гиперссылка на живой сайт (`siteUrl`, домен без протокола, `↗`, target=_blank).
- **Обзор** — `lead`-абзац (`overview`): что за проект / задача / результат.
- **Контент-блоки** — гибкий массив `blocks: CaseBlock[]` (`works.ts`), рендер по порядку через единый поток `sections` (обзор + блоки), каждая секция — `.band` (паддинг 48px верт., боковые 15px). Типы блоков: `text` (опц. heading + body), `image` (во всю ширину секции, AR 16:9; нет `src` → плейсхолдер `surface`-цветом), `quote`. Картинки внутри кейса готовить ~2000px по длинной стороне, WebP. **Фон страницы белый, текст чёрный** (чередование полос белый↔цвет пробовали — выглядит «как флаг в полоску», отклонено).
- **CTA-блок** — синяя секция (accent), белый текст: «Хотите такой же сайт?» + ссылки на автора (Telegram — пока заглушка `#`, почта `chendev2003@gmail.com`, тел `+7 953 121 74 77`).
- **Навигация** — пред./след. кейс (циклично), 2 колонки на десктопе.
- **Метрик НЕТ** (для дизайн-портфолио натянуты — выкинуты осознанно).
- **Hero-обложки на экспорт:** десктоп 2400×1350 (16:9) WebP q≈78; при арт-дирекшне мобилка 1200×1600. Видео — `muted loop playsinline`, без звука.

### About

2 колонки: слева заголовок h2, справа — био (lead) + 3 группы навыков.

### Process

4 шага. Каждый: верхняя линия, крупный номер **синим** (accent), заголовок h3, текст.

### Contact

Синий фон. Заголовок h1 с **белой** highlight-плашкой. Лид + белая кнопка + текстовые ссылки.

### Footer

Сплошная верхняя линия. 3 колонки: бренд+тэглайн, навигация, контакты. Снизу мета (mono).

## Структура проекта

```
src/
├── components/    компоненты-секции, по одной на блок главной
├── data/          works.ts — массив кейсов
├── layouts/       Layout.astro — base shell, шрифты, ClientRouter
├── pages/
│   ├── index.astro      главная (живой полигон дизайн-системы)
│   └── works/[slug].astro  кейс
└── styles/global.css     токены @theme + примитивы
```

## Do / Don't

_TBD — собираем по ходу._
